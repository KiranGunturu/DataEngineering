EMR
=====

version - emr-6.4.0
spark 3.1.2
hadoop distribution - amazon 3.2.1
python 3.7


VM

There will be big server (which holds lot of resources)

128 GB RAM
4 TB Hard disk
32 cpu cores

4 small machines

32 gb ram
1 tb hard disk
8 cpu cores

this machine is called as VM.

in 1 big physical machine 4 smaller logical machines are running.

that is why it is called a VM.

on-premise
===========

this is very challenging process.

Infra maintainance
Infra deployement
Infra monitoring

3 imp vendors
=============
1.cloudera
2.Hortonworks
3.MapR

1. Amazon AWS (EMR) 

big data hadoop services fully managed by aws on cloud.

2. Microsoft Azure (HD Insight)

3. Google Cloud (DataProc)

4. HDFS vs S3

	s3 is distributed file system fully managed by AWS.

	4 machines - VMs

	each of these VMs has some RAM, cpu cores, storage

	I store a 2 gb file on HDFS.


	start cluster
	spark code reads file from HDFS
	do some processing 
	and write result back to HDFS
	terminate the cluster

	your HDFS data will be gone or lost.

	if we do not terminate the cluster we incurr huge bills for the cluster charges

	the solution is S3.

	take the data from S3

	put to HDFS

	do the processing in spark

	put the output back to S3 and then shut down your cluster.

	s3 storage is decoupled from our cluster.
	
5. there are 3 kinds of instances in amazon aws.

	1. on-demand instances
		procure, use and shutdown - pay hourly basis
	2. spot instances
		aws gives this on a heavy discounted price. upto 90% discount compared to on demand.
		but aws can take it back whenever it wants by just giving a 2 min prior notification
	3. reserved instances
		we have to commit upfront that I will use this resource for a long time. and then aws gives some discounts as we commited for long time.
		
6. There are 3 kinds of nodes we can have in cluster

	1. Master - Manages the cluster
	2. Data Node - each cluster has one or more core nodes
		It is the one hosts our HDFS data and also it is capable to run tasks
	3. Task Node - These can only run tasks and no HDFS on it.
		if our application is compute heavy then we can opt for few task nodes which can add more computing power to the cluster.
		in this case spot instances are the best choice.
		
7. Transient vs long running cluster

	Transient is the one which automatically terminates when all the steps are done. - called as step execution
	
	Long running cluster is the one where we have to manually terminate the cluster.

In amazon S3 we need to create a bucket and upload file called file.txt

val input = sc.textFile("s3n://bucketname/file.txt") - read file from S3

create cluster

security groups for master - click on master - inbound - edit - add rule - choose ssh - port as 22 - my ip or anywhere - give ip address

ssh -i DB100.pem hadoop@ec2-13-232-140-185.ap-south-1.compute.amazonaws.com // ssh to master node

here hadoop is the username


spark-shell - for spark scala terminal

:q to exit


I want to bring the jar to master

	1. first put to s3
	2. then get from s3 to master node
	
aws s3 cp s3://bucketname/wordcount20.jar . // download file from s3 to master node current dir

the history server should be configured on port 18080 and we should allow traffic.

security groups for master - click on master - inbound - edit - add rule - give port as 18080 - my ip or anywhere

spark-submit --class wordCount wordcount20.jar
	
ec2-13-232-140-185.ap-south-1.compute.amazonaws.com:18080 - put this in chrome to see spark history server

Jobs - Stages - Storage - Environment - Executors // Spark UI

fundamentals of big data
========================

1.Types of instances
====================
	
	starts with m - general purpose
	starts with c (c5.large) - compute optimized - no of cores will be more - ML purpose
	starts with r (r5.large) - memory optimized - spark purpose
	
2. creating a VM
================

A VM which is empty + operating system/some softwares

OS / some softwares - AMI

AMI - Amazon machine image // can choose linux to be installed 

3. Key Pair
===========

public and private key

public is kept on the VM
private key is the kept on local machine and we need to use this to connect to VM.

public is kept in .ssh directory on the VM
preivate key is nothing but a .pem file

4. Elastic IP
==============

from outside world if we want to connect to AWS ec2 instance then we use public ips
with in the aws if we want to connect to different ec2 instances then we use private ips

private ips wont change untill we terminate the machine

if we stop the VM and start then private ip will remain the same however public ip will change

if public ip is changing everytime then we have to modify our ssh command to use the latest public ip while connecting.

if we do not want any changes in terms of connecting to VM then elastic ip comes handy.

elastic ip is the wrapper on public ip

create elastic ip and associate your ec2 instance then we get elastic ip - use that to connect to VM instead of public ip

elastic ip
===========
10.18.23.45

public ip
10.78.56.17

after stop and restart

public ip
10.78.57.83


Storages
=========

Instance store
	refers to the storage which is tightly coupled with ec2
	it is local to ec2 instance
	when you terminate the machine then the data will be lost
	
S3 simle storage service
	outside of cluster
	can be accessed it from anywhere
	decouples the data from cluster
	
	
EBS
	elastic block storage
	network attached storage accessibale from ec2 instance using the mount.
	when we terminate the cluster we have a option that whether this data should be deleted or not.


AWS CLI
=========
to automate the things like VM creation, executing workload, starting cluster, terminating cluster,configuring the size
we can do all these either with AWS CLI or boto3

aws
aws help

aws s3 mb s3://itv-github-emr --region us-east-1 //to create bucket
aws s3 ls|grep github
aws s3 ls itvgithub-emr --recursive
wget https://data.gharchive.org.2021-01-13-{0..23}.json.gz
aws s3 \
	cp . s3://itv-github-emr/prod/landing/ghactivity/ \
	--exclude "*" \
	--include "2021-01-13*" \
	--recursive //it will create if target folders are not exists in s3 bucket
aws s3 ls s3://itv-github-emr/prod/landing/ghactivity/


zip -r itv-githubactivity.zip process.py read.py util.py write.py

scp -i ~/.ssh/creds.pem itv-githubactivity.zip ec2-user@ec2-54-197-15-11.compute-1.amazonaws.com~/itvghactivity //it will copy the file from local to itvghactivity folder on master emr node


scp -i ~/.ssh/creds.pem app.py ec2-user@ec2-54-197-15-11.compute-1.amazonaws.com~/itvghactivity //it will copy the app.py file to the itvghactivity folder on master emr node

before entering into pyspark terminal on emr we have to make sure the user we connect to emr has user space (permissions to launch the applications)

try entering into pyspark terminal on emr and see if can go through if not check users

hdfs dfs -ls /user

this should show the user by which we connected to the emr

sudo -u hdfs hdfs dfs -mkdir /user/ec2-user
now we should make sure owner of folder ec2-user is the ec2-owner itself not the hdfs
hdfs dfs -ls /user
sudo -u hdfs hdfs dfs -chown -R ec2-user:ec2-user /user/ec2-user
hdfs dfs -ls /user
now we can lauch pyspark without any issues.

to submit a spark job on emr client mode
========================================
export ENVIRON=PROD
export SRC_DIR=s3://itv-github-emr/prod/landing/ghactivity/
export SRC_FILE_FORMAT=json
export TGT_DIR=s3://itv-github-emr/prod/raw/ghactivity/
export TGT_FILE_FORMAT=parquet
env // to review all environment variables on emr master node
export SRC_FILE_PATTERN=2021=01-13
histroy|grep export //to review the environemnts which we set with export command

spark-submit \
	--master yarn \
	--py-files itv-githubactivity.zip \
	app.py
	
now we can open the jupyter lab under EMR console

choose kernal as pyspark
spark /to see the spark session
df = spark.\
	read.\
	parquet('s3://itv-github-emr/prod/raw/ghactivity/')


we can choose auto terminate after certain time.

how do we connect to the master node of EMR
==========================================

ssh -i ~/.ssh/creds.pem ec2-user@ec2-54-197-15-11.compute-1.amazonaws.com

ec2-54-197-15-11.compute-1.amazonaws.com - public DNS and address of master node

spark-submit --help

to submit a spark job on emr cluster mode
========================================

spark-submit \
	--master yarn \
	--deploy-mode cluster \
	--conf "spark.yarn.appMasterEnv.ENVIRON=PROD" \
	--conf "spark.yarn.appMasterEnv.SRC_DIR=s3://itv-github-emr/prod/landing/ghactivity/" \
	--conf "spark.yarn.appMasterEnv.SRC_FILE_FORMAT=json" \
	--conf "spark.yarn.appMasterEnv.TGT_DIR=s3://itv-github-emr/prod/raw/ghactivity/" \
	--conf "spark.yarn.appMasterEnv.TGT_FILE_FORMAT=parquet" \
	--conf "spark.yarn.appMasterEnv.SRC_FILE_PATTERN=2021=01-13" \
	--py-files s3://itvgithub/itv-githubactivity.zip \
	s3://itvgithub/app.py

step execution in already running cluster with client mode
=============================================================
under emr console
add steps
step as spark or steaming application
we can give name
deploy mode as client
provide spark-submit options as bootstrapping action (keep all export commands in a file in s3 and pass that file) so when the cluster is running
	all those will be added
application location can be given as local or also s3 path(keep zip and app.py files under same bucket or folder)
pass if any args needed
fail on action can be continue or terminate the cluster or cancel and wait



step execution in already running cluster with cluster mode
=============================================================
under emr console
add steps
step as spark or steaming application
we can give name
deploy mode as cluster
provide spark-submit options as below
	--conf "spark.yarn.appMasterEnv.ENVIRON=PROD"
	--conf "spark.yarn.appMasterEnv.SRC_DIR=s3://itv-github-emr/prod/landing/ghactivity/"
	--conf "spark.yarn.appMasterEnv.SRC_FILE_FORMAT=json"
	--conf "spark.yarn.appMasterEnv.TGT_DIR=s3://itv-github-emr/prod/raw/ghactivity/"
	--conf "spark.yarn.appMasterEnv.TGT_FILE_FORMAT=parquet"
	--conf "spark.yarn.appMasterEnv.SRC_FILE_PATTERN=2021=01-13"
	--py-files s3://itvgithub/itv-githubactivity.zip
application location can be given  s3 path(keep app.py and itv-githubactivity.zip files under same bucket or folder)
pass if any args needed
fail on action can be continue - it will go to the next step if any
					or terminate the cluster - cluster will be terminated
					or cancel and wait - it will just wait where it failed

once all the steps are completed, cluster will be terminated and we can't validate the data using jupyter unless we provision the cluster again

for this we can create an external table on top of s3 data and can go for athena to validate the same.


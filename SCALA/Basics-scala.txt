======
scala
======

1. var vs val

int a = 5;  //Java

val a: Int = 5  //Scala

here a is constant so reassignment is not possible

var b: Int = 10

b is now a variable so this can be reassigned.

object variables {
	
	val a: String = "Hello"

	println(a)
	
	val numberOne: Int = 5 or 
	
	val numberOne = 5 //scala can infer the datatype here as Int
	
	val c: Boolean = true
	
	val d: Char = 'a'
	
	val pi: Double = 3.1415
	
	double takes - 8 bytes as it can store more values than float in precision (very accurate)
	
	float takes - 4 bytes
	
	val piSinglePrecistion: Float = 3.1415f
	
	val e: Long = 1234345435435l ( L at the end)
	
	val smallNumber: Byte = 127 ( -128 to 127)
	
	println("combined results: " +numberOne + c + d + pi)
	
	}
	
==============
Types in scala
==============

===s interpolation====  substritute the varibale ( string interpolation)

var name: String = "Kiran"
println(s"hello $name how are you")


===F interpolation====

val pi: Double = 3.14255

println("value of pi is $pi%.3f") //it will print 3 decimal values 

====raw interpolation========

println("hello how \n are you") // to print the string as raw now in this case:

hello how
are you

println(raw"hello how \n are you") // it will now print as is: hello how \n are you


========================
== in Java versus scala
========================

In java to compare 2 strings we have to use equals methos.

also in java == is for reference comparison ( like if we two objects are pointing to samething)

val x: String = "Kiran"
val y: String = "Kiran"

val z = x == y // true in scala and false in java

===================
if (1>3) 
{
println("hello")
}
else
{
println("there")
}


if we have only single statement inside the loop then we dont need curly braces.


if (1>3) 
println("hello")
else
println("there")

	
==================
MATCH CASE (SWITCH IN JAVA)
==================

val num = 1

num match {
case 1 => println("one")
case 2 => println("two")
case 3 => println("three")
case _ => println("something else")
}



==============
FOR LOOP
==============

for (x <- 1 to 10) {
val squared = x*x
println(squared)
}

==============
WHILE LOOP
==============

var x = 0
while(x <= 10) {
println(i)
i=i+1
}

==============
DO WHILE LOOP   -- code will execute atleast once
==============

i = 0
do {
println(i)
i=i+1
} while (i <= 10)


=================

val var1 =
{
val x = 10
x + 20
7
}

println(var1)

In a block, last statement is return value. now in this case o/p of this block will be 7


#deal with dates when coming from file they will come as INT

we will have to convert the int value to string and then back to Date

In_Data.select(to_date(col("rechargedDate").cast("string"),"yyyymmdd")).show()
select city,count(*) as total
from orders
inner join customers
on orders.customer_id = customers.customer_id
where order_status!='PENDING_PAYMENT'
group by city
having count(*) > 10
order by total desc

from,join

where (where can happen after from as well)

group by

having

select (cols listed and all the aliases will happen here)

order by

limit


select city,count(*) as total
from orders
inner join customers
on orders.customer_id = customers.customer_id
where order_status!='PENDING_PAYMENT'
group by city
having city='Franklin'
order by total desc


from,join

where (where can happen after from as well)

group by

select (cols listed and all the aliases will happen here)

order by

limit

here there is no having clause because system will push the filter after from clause.(predicate pushdown) wheare as in above class it can'table
as aggregation should happen first( count(*) )